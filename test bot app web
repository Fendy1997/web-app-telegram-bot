<!DOCTYPE html>
<html>
<head>
    <title>Dating App</title>
    <meta charset="UTF-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background: linear-gradient(45deg, #FF6B6B, #4ECDC4);
            height: 100vh;
        }
        
        .container {
            max-width: 400px;
            margin: 0 auto;
            background: white;
            border-radius: 20px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }
        
        .profile {
            text-align: center;
            margin: 20px 0;
        }
        
        .profile-img {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            object-fit: cover;
            margin-bottom: 15px;
        }
        
        .buttons {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 30px;
        }
        
        .btn {
            padding: 15px 30px;
            font-size: 18px;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            font-weight: bold;
        }
        
        .like {
            background: #FF6B6B;
            color: white;
        }
        
        .dislike {
            background: #4ECDC4;
            color: white;
        }
        
        h1 {
            color: #333;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🔥 Знакомства</h1>
        
        <div class="profile">
            <img src="https://images.unsplash.com/photo-1494790108755-2616b612b786?ixlib=rb-1.2.1&auto=format&fit=crop&w=500&q=80" 
                 class="profile-img" 
                 alt="Фото профиля">
            <h2>Анна, 24</h2>
            <p>Люблю путешествия, кофе и хорошие книги</p>
            <p>📍 Москва</p>
        </div>
        
        <div class="buttons">
            <button class="btn like" onclick="sendLike()">❤️ Лайк</button>
            <button class="btn dislike" onclick="sendDislike()">👎 Дизлайк</button>
        </div>
        
        <div style="text-align: center; margin-top: 20px;">
            <button onclick="window.Telegram.WebApp.close()" 
                    style="background: #999; color: white; padding: 10px 20px; border: none; border-radius: 10px;">
                Закрыть
            </button>
        </div>
    </div>

    <script>
        // Telegram API
        const tg = window.Telegram.WebApp;
        
        // Растягиваем на весь экран
        tg.expand();
        
        // Получаем данные пользователя из Telegram
        const user = tg.initDataUnsafe.user;
        
        // Если есть данные пользователя, можно их использовать
        if (user) {
            console.log("Привет, " + user.first_name);
        }
        
        function sendLike() {
            // Отправляем данные в бота
            tg.sendData(JSON.stringify({
                action: 'like',
                userId: user ? user.id : null,
                timestamp: new Date().getTime()
            }));
            alert('❤️ Лайк отправлен!');
        }
        
        function sendDislike() {
            tg.sendData(JSON.stringify({
                action: 'dislike',
                userId: user ? user.id : null,
                timestamp: new Date().getTime()
            }));
            alert('👎 Дизлайк отправлен!');
        }
        
        // Когда приложение закрывается
        tg.onEvent('viewportChanged', () => {
            console.log('Приложение закрывается');
        });
    </script>
</body>
</html>
