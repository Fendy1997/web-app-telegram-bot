<!DOCTYPE html>
<html>
<head>
    <title>🔥 Знакомства</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .app {
            background: white;
            border-radius: 20px;
            padding: 30px;
            text-align: center;
            max-width: 400px;
            width: 100%;
            box-shadow: 0 20px 40px rgba(0,0,0,0.2);
        }
        h1 {
            color: #333;
            margin-bottom: 30px;
            font-size: 28px;
        }
        .photo {
            width: 180px;
            height: 180px;
            border-radius: 50%;
            object-fit: cover;
            margin: 0 auto 20px;
            display: block;
            border: 5px solid #764ba2;
        }
        .name {
            font-size: 24px;
            font-weight: bold;
            color: #333;
            margin: 10px 0;
        }
        .info {
            color: #666;
            margin: 10px 0;
            font-size: 18px;
        }
        .buttons {
            margin-top: 30px;
            display: flex;
            gap: 15px;
            justify-content: center;
        }
        button {
            padding: 15px 30px;
            font-size: 18px;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            font-weight: bold;
            transition: 0.3s;
        }
        .like {
            background: #FF4757;
            color: white;
        }
        .dislike {
            background: #2ED573;
            color: white;
        }
        .like:hover, .dislike:hover {
            transform: scale(1.05);
        }
    </style>
</head>
<body>
    <div class="app">
        <h1>🔥 Знакомства</h1>
        
        <img src="https://images.unsplash.com/photo-1494790108755-2616b612b786?ixlib=rb-1.2.1&auto=format&fit=crop&w=500&q=80" 
             class="photo" 
             alt="Фото">
        
        <div class="name">Анна</div>
        <div class="info">24 года • Москва</div>
        <div class="info">Люблю путешествия и кофе</div>
        
        <div class="buttons">
            <button class="like" onclick="sendLike()">❤️ Лайк</button>
            <button class="dislike" onclick="sendDislike()">👎 Дизлайк</button>
        </div>
    </div>

    <script>
        // Проверяем, открыто ли в Telegram
        if (typeof window.Telegram !== 'undefined' && window.Telegram.WebApp) {
            const tg = window.Telegram.WebApp;
            tg.expand(); // На весь экран
            tg.ready(); // Готово к работе
        }
        
        function sendLike() {
            alert('❤️ Лайк отправлен!');
            if (window.Telegram && window.Telegram.WebApp) {
                window.Telegram.WebApp.sendData(JSON.stringify({action: 'like'}));
                setTimeout(() => window.Telegram.WebApp.close(), 1000);
            }
        }
        
        function sendDislike() {
            alert('👎 Дизлайк отправлен!');
            if (window.Telegram && window.Telegram.WebApp) {
                window.Telegram.WebApp.sendData(JSON.stringify({action: 'dislike'}));
                setTimeout(() => window.Telegram.WebApp.close(), 1000);
            }
        }
    </script>
</body>
</html>
